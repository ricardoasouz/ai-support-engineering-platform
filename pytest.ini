[pytest]
testpaths = tests integration_tests
markers =
    unit: fast, deterministic tests with no network or Docker requirement
    integration: tests against the isolated PostgreSQL and Kafka Compose stack
    e2e: complete incident-to-resolution workflow tests
